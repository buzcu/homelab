# stirling-pdf

Review the current upstream configuration before enabling authentication or
public access.

Note the image: `frooodle/s-pdf` is the legacy repository name. Upstream has
moved to `stirlingtools/stirling-pdf`. The pinned tag still exists, but plan a
move before the next upgrade — change it in `config/versions.yml` and
`compose.yml` together.
